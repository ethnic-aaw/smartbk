# Letakkan header CORS di api/index.php agar dinamis (same-origin only).
# File ini hanya melarang akses langsung ke index.php api info.
<Files "index.php">
    Order Allow,Deny
    Allow from all
</Files>